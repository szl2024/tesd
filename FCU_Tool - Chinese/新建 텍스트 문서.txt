1.M1中模型的runnable的名字和模型名字不一致的时候应该记录模型的名字。(即system_root中的subsystem的名字和模型的名字不一样)
2.csv的分析逻辑中不应该是两个一起分析，而应该单一分析，即port的类型 p和 r不应该一起分析。
3. M3和M4 M6中因为需要axw.csv的出发和到达地的信息，因为asw.csv文件读取失败，所以不能够记录
4. M2的计算方式修改



1.已修改
2. csv的分析中目前是根据DE_OP来进行分组的，即根据DE_OP相同的话进行分组到一起，再进行C-S，S-R关系的分析。但是目前不知道服务端和接收端是否会分到同一组里，我推测是因为测试的时候并非真实连接的关系，所以DE_OP不在同一组。
3.未知，因为对asw的分析好像并没有错。
4. M2的计算方式没错，是通过lattix的script代码方式进行计算并解决的